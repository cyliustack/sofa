#!/bin/bash
if [[ $(which python3.5) ]] ; then
	python3.5 $(dirname $0)/sofa.py "$@"
elif [[ $(which python3.6) ]] ; then
	python3.6 $(dirname $0)/sofa.py "$@"
else
	echo "No compatible python is found. Please run sofa/tools/prerequisite.sh for complete installation."
fi 
